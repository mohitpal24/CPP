#include <bits/stdc++.h>
using namespace std;

int main(){
    char a;
    cin>>a;
    if(a>='a' && a<='z' || a>='A'&& a<='Z'){
        cout<<"the character is alphabet";

    }
    else{
        cout<<"the character is not alphabet";
    }
    return 1;
}
